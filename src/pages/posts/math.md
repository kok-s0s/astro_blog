---
layout: ../../layouts/Post.astro
title: '复健'
pubDate: 2025-12-03
updatedDate: 2025-12-03
description: '正如此标题'
author: 'kok-s0s'
image:
  url: '/images/math/brain.png'
  alt: 'Astro'
tags: ['Math']
---

## 机器人坐标系中的向量变换

已知一个 3D 点在工具坐标系（TCP 坐标系）下的坐标为：

$$p_{tcp} = \begin{bmatrix}0.3 \\ -0.1 \\ 0.2\end{bmatrix}$$

工具坐标系相对于基坐标系的姿态由旋转矩阵 $R$ 给出：

$$R = \begin{bmatrix}0 & -1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & 1\end{bmatrix}$$

工具坐标系在基坐标系中的位置为：

$$t = \begin{bmatrix}0.5 \\ 0.4 \\ 0.2\end{bmatrix}$$

求该点在**基坐标系**下的坐标，计算公式为：
$$p_{base} = R \cdot p_{tcp} + t$$

---

需完成的计算步骤

1. **先计算** $R \cdot p_{tcp}$
2. **再加上** 平移向量 $t$

旋转矩阵：

$$R = \begin{bmatrix} 0 & -1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & 1\end{bmatrix}$$

点：

$$p_{tcp} = \begin{bmatrix} 0.3 \\ -0.1 \\ 0.2\end{bmatrix}$$

逐行点乘（像 C++ for 循环一样拆）：

|  行号  |                  计算公式                   | 结果  |
| :----: | :-----------------------------------------: | :---: |
| 第一行 | $0 \cdot 0.3 + (-1)\cdot(-0.1) + 0\cdot0.2$ | $0.1$ |
| 第二行 |   $1\cdot0.3 + 0\cdot(-0.1) + 0\cdot0.2$    | $0.3$ |
| 第三行 |   $0\cdot0.3 + 0\cdot(-0.1) + 1\cdot0.2$    | $0.2$ |

所以：

$$R p_{tcp} = \begin{bmatrix} 0.1 \\ 0.3 \\ 0.2\end{bmatrix}$$

**再加平移向量 (t)**

$$p_{base} = R p_{tcp} + t$$

$$\begin{bmatrix}0.1 + 0.5 \\ 0.3 + 0.4 \\ 0.2 + 0.2\end{bmatrix}=\begin{bmatrix}0.6 \\0.7 \\0.4\end{bmatrix}$$

🎉 **最终正确答案**

$$\boxed{p\_{base} = [0.6,\ 0.7,\ 0.4]}$$
